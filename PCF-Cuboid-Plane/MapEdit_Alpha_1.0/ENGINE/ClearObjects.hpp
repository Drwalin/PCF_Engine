


void ResizeCollider()
{/*
    trigger.resize(XTRIGGER);
    for(unsigned int x=0; x<XTRIGGER; x++ )
    {
        trigger[x].resize(ZTRIGGER);
    }*/
}


void ClearObjects()
{/*
    trigger.resize(XTRIGGER);
    for(unsigned int x=0; x<XTRIGGER; x++ )
    {
        trigger[x].resize(ZTRIGGER);  
    }*/
    
    object.resize(1);

    objectmodel.resize(0);
    environmentmodel.resize(0);
    environment.resize(0);
}



